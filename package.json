{
  "name": "store-rate-backend",
  "version": "1.0.0",
  "description": "Store Rating Platform Backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}